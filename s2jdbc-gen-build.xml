<?xml version="1.0" ?>
<project name="s2jdbc-gen">

    <taskdef resource="s2jdbc-gen-task.properties" classpath="${classpath}"/>

    <target name="gen-ddl" depends="classes">
        <gen-ddl
            classpathdir="target/classes"
            rootpackagename="sample"
            classpath="${classpath}"
            dump="true" />
    </target>

    <target name="gen-entity" depends="classes">
        <gen-entity
            rootpackagename="sample"
            classpath="${classpath}" />
    </target>

    <target name="migrate" depends="gen-ddl">
        <migrate
            classpathdir="target/classes"
            rootpackagename="sample"
            classpath="${classpath}" />
    </target>
</project>
